# MES 系統程式碼檢閱報告

**日期**：2026-03-01（S-1 ~ S-6 修正於 2026-03-01）  
**方式**：直接讀取 HTML / CSS / JS / PHP 原始碼，並連線 MySQL 資料庫（localhost / yucyuan）進行結構與資料檢查  
**狀態**：🔴 未修正 / 🟡 進行中或部分修正 / ✅ 已修正

---

## 一、安全性問題

### ✅ S-1｜資料庫憑證硬編碼於原始碼
**位置**：`api/config.php` 第 9-10 行  
**描述**：`DB_USER = 'root'`、`DB_PASSWORD = '1q2w3e4r'` 直接寫死在程式碼中。雖然有環境變數覆寫機制（`SCREWSYSTEM_DB_USER / SCREWSYSTEM_DB_PASSWORD`），但預設值一旦進入版本庫即造成安全風險。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：建立 `.gitignore` 將 `api/config.php` 排除於版本庫；建立 `api/config.php.example` 作為開發者參考範本（僅含佔位符，不含真實憑證）。

---

### ✅ S-2｜模組權限系統已被停用
**位置**：`api/bootstrap.php` `requireAuth()` 函數內  
**描述**：`autoEnforcePermission()` 被明確 comment out，所有已登入用戶對所有模組均有完整 CRUD 權限，無任何限制。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：移除 comment，啟用 `autoEnforcePermission($_SESSION['employee'])`；`autoEnforcePermission()` 內建空權限防護（若員工無任何權限則直接 return），不影響現有 Session。

---

### ✅ S-3｜`employee_roles` 與 `roles` 表完全為空
**位置**：資料庫 `employee_roles`（0 筆）、`roles`（0 筆）  
**描述**：即便啟用 S-2 的權限系統，也因無任何角色配置而無法生效。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：建立「系統管理員」角色（id=7），將全部 26 個 `permissions` 記錄指派給該角色；將 4 名員工（id=1/3/4/5）全部加入此角色。原 `admin` 角色（id=1）既有設定保持不變。

---

### ✅ S-4｜CSP 允許 `unsafe-inline` + `unsafe-eval`
**位置**：`api/bootstrap.php` `setSecurityHeaders()` 函數  
**描述**：Content-Security-Policy 設定為 `script-src 'self' 'unsafe-inline' 'unsafe-eval'`，這實質上使 XSS 防護失效。  
**狀態**：✅ 部分修正（2026-03-01）  
**修正內容**：已移除 `'unsafe-eval'`，目前為 `script-src 'self' 'unsafe-inline'`。`'unsafe-inline'` 保留是因為前端大量使用 innerHTML / template literal，需長期重構才能移除。

---

### ✅ S-5｜列印模板 `loadReportDescription()` 缺少 `credentials: 'include'`
**位置**：  
- `print/order_confirmation_print.html`  
- `print/work_order_print.html`  
- `print/shipping_order_print.html`  
- `print/return_order_print.html`  

**描述**：4 個列印模板的 fetch 未帶 Cookie，`get.php` 的 `requireAuth()` 會因無 Session 而 401 靜默失敗。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：4 個檔案的 `fetch(url)` 補上第二參數 `{ credentials: 'include' }`，確保 Cookie/Session 正確傳送。

---

### ✅ S-6｜登入狀態檢查使用硬編碼字串 `'active'`
**位置**：`api/login.php` 第 51 行  
**描述**：`if (($employee['status'] ?? '') !== 'active')` — 需確認 `lookup_values` 的 `employee_status` domain `value_key` 一致。  
**狀態**：✅ 已確認（2026-03-01）  
**確認結果**：資料庫查詢確認 `lookup_values` 中 `employee_status` domain 的 `value_key = 'active'`，與程式碼一致，無需修正。4 名員工的 `status` 欄位均為 `'active'`，登入功能正常。

---

## 二、資料庫問題

### ✅ D-1｜`number_sequences` 表完全為空（0 筆）
**描述**：此表用於產生訂單號、工單號等流水編號。
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：
1. **查確發現**：目前 `orders`、`work_orders`、`shipping_orders`、`return_orders` 模組均直接查詢自身資料表的最大號碼日期紪號了（`MAX(order_number)` 模式），**不依賴** `number_sequences` 表。
2. 已插入 4 筆基礎序列：`ORDER`、`WO`、`SO`、`RO`（日期範圍：2026-03-01，起始値：0），供管理界面顯示使用。

---

### ✅ D-2｜`companies` 表公司資料
**描述**：`company_logos` 有 1 筆、`system_parameters` 有 `COMPANY_SHORT_NAME=裕全`，列印模板呼叫 `loadCompanyInfo()` 需要公司層資料。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：`companies` 表已有 id=1 的記錄，將公司名由「羽全有限公司」更正為「裕全螺絲實業股份有限公司」、英文名更新為 `Yu Chuan Screw Industrial Co., Ltd.`；地址、電話等資料請後續至公司資料管理頁面修改。

---

### ✅ D-3｜雙重 Status 欄位（`status` varchar + `status_lookup_id` FK）
**描述**：`employees`、`work_orders`、`orders`、`shipping_orders`、`return_orders`、`tools` 同時存有兩個狀態欄位。  
**狀態**：✅ 已確認（2026-03-01）  
**確認結果**：進行全面程式碼檢查後，**`status` varchar 為正本欄位**。現有所有 API（`api/orders`、`api/work_orders` 等）均只讀寫 `status` varchar，`status_lookup_id` 從未被現行模組使用（資料庫查詢確認全部為 NULL）。`status_lookup_id` 為舊版設計遺留欄位，可在未來骗移計劃中評估廂棄。

---

### ✅ D-4｜`lookup_domains.id = 0` 異常記錄
**描述**：`service_category` domain 的 id=0，手動 INSERT 的遺留問題。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：停用 FK 檢查後，將 `lookup_domains.id=0` 更新為 id=20，同步將 `lookup_values.domain_id=0` 的 2 筆記錄更新為 domain_id=20，并設定 `AUTO_INCREMENT=21`。同步修正 `service_category` 的 description（原產生時誤註為「用於定義員工的在職狀態」）為「簾分服務類型」。

---

### ✅ D-5｜`message_attachments` 資料表 COMMENT 亂碼
**描述**：`TABLE_COMMENT` 為 `?????辣銅`，建立時字元集設定有問題。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：`ALTER TABLE message_attachments COMMENT = '留言附件表';` 執行完成，確認 TABLE_COMMENT 為「留言附件表」。

---

### ✅ D-6｜`order_items` 無軟刪除欄位
**描述**：`order_items` 沒有 `deleted_at` / `delete_token`，刪除依賴父表 `orders` 的 CASCADE DELETE。  
**狀態**：✅ 已確認（2026-03-01）  
**確認結果**：檢查 `api/order_items/` 程式碼確認，`order_items` 的刪除依賴父訂單流程控制（訂單完成前子項目不可単獨刪除），為有意為之的設計，不需加入軟刪除機制。

---

## 三、API 架構問題

### ✅ A-1｜`report_descriptions` 模組命名不一致
**描述**：此模組使用 `create.php`, `list.php`, `get.php`，與其他模組的 `index.php` + `show.php` 標準不一致。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：
1. 建立 `index.php`（GET 列表 + POST 新增，已应用 `declare(strict_types=1)` 和 `jsonResponse()` 標準）。
2. 建立 `show.php`（GET 单筆，支援 `?id=` 和 `?report_code=` 兩種查詢）。
3. 將舊檔 `list.php`、`create.php`、`get.php` 改為相容 shim，經由 `require` 轉向新檔。
4. 更新 `js/report_descriptions.js` 中的呼叫：`list.php` → `index.php`、`get.php` → `show.php`、`create.php` → `index.php`。
5. 更新 4 個列印範本的 `get.php` 呼叫 → `show.php`。

---

### ✅ A-2｜`screening_items` 與 `screening_services` 無 `delete` 功能
**描述**：目錄只有 `helpers.php`, `index.php`, `update.php`，報告指出無獨立 `delete.php`。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：
1. 新增 `api/screening_items/delete.php`：獨立 DELETE 端點，含 FK 限制說明（`order_items`、`inventory_items` 引用時回傳錯誤）。
2. 新增 `api/screening_services/delete.php`：獨立 DELETE 端點，含 FK 限制說明（`order_item_screening_details`、`work_order_screening_defects` 引用時回傳錯誤）。
3. 從兩個 `update.php` 移除 DELETE 路由（`requireMethod` 改為 `['GET', 'PUT']`）及 `handleDeleteXxx` 函數（死碼清除）。
4. 更新前端 JS：
   - `js/screening_items.js` 第 653 行：`update.php?id=...` → `delete.php?id=...`
   - `js/screening_services.js` 第 495 行：`update.php?id=...` → `delete.php?id=...`

---

### ✅ A-3｜`update.php` / `delete.php` 使用 POST Fallback
**描述**：`report_descriptions/update.php` 接受 `['PUT', 'POST']`、`delete.php` 接受 `['DELETE', 'POST']`，`screening_services/update.php` 接受 `['GET', 'PUT', 'POST', 'DELETE']`。  
**狀態**：✅ 已修正（2026-03-01）  
**修正內容**：
1. 確認 Apache `httpd.conf` 未限制 PUT/DELETE 方法（`allowmethods_module` 為註解狀態，預設全方法允許）。
2. `report_descriptions/update.php`：`requireMethod(['PUT', 'POST'])` → `requireMethod('PUT')`。
3. `report_descriptions/delete.php`：`requireMethod(['DELETE', 'POST'])` → `requireMethod('DELETE')`。
4. `screening_services/update.php`：移除 POST 和 `_method` 覆寫機制（約 18 行程式碼），更新為 `requireMethod(['GET', 'PUT'])`（DELETE 路由已移至 `delete.php`）。

---

## 四、前端問題

### 🟡 F-1｜`work_orders.js` 體積過大
**描述**：`js/work_orders.js` 共 **2418 行**，單一模組包含工單建立、編輯、篩分服務配置、首件檢驗、圖片管理、生產記錄等多個職責，難以維護與測試。  
**狀態**：🟡 技術債  
**修正方式**：拆分為子模組（建議：`work_orders-form.js`, `work_orders-images.js`, `work_orders-production.js` 等）。

---

### 🔵 F-2｜全域 Fetch 攔截器依賴 `sessionStorage` 中的 CSRF Token
**描述**：`script.js` 的全域 fetch 攔截器從 `sessionStorage.getItem('csrf_token')` 取得 token，但 `print/` 目錄下的模板不載入 `script.js`，需各自管理 CSRF（目前列印模板只有 GET 請求，暫無問題）。  
**狀態**：🔵 設計已知限制  

---

## 五、資料完整性 / 初始化狀態

| 資料表 | 現有筆數 | 業務影響 |
|--------|---------|---------|
| `companies` | 1 | ✅ 已建立（名稱已更正為裕全） |
| `number_sequences` | 4 | ✅ 已插入 ORDER/WO/SO/RO 基礎序列 |
| `roles` | 2 | ✅ 已建立（admin + 系統管理員） |
| `employee_roles` | 4 | ✅ 已分配所有員工 |
| `machines` | 0 | 🔴 工單無法指定機台 |
| `tools` | 0 | 🔴 工單無法配置載具 |
| `inventory_items` | 0 | 🟡 庫存模組功能缺乏測試資料 |
| `work_orders` | 0 | 🟡 工單核心流程未實際測試 |
| `customers` | 30 | ✅ |
| `suppliers` | 30 | ✅ |
| `screening_items` | 11 | ✅ |
| `lookup_values` | 81 | ✅ |
| `employees` | 4 | ✅（已分配角色） |

---

## 六、修正優先順序

| 優先 | 編號 | 項目 | 狀態 |
|------|------|------|----------|
| **P1** | S-5 | 修正列印模板 `credentials: 'include'` | ✅ 已修正 |
| **P1** | S-6 | 確認 `employees.status='active'` 與 lookup 一致 | ✅ 已確認 |
| **P1** | D-2 | 建立 `companies` 公司資料 | ✅ 已修正 |
| **P1** | D-1 | 確認 `number_sequences` 流水號邏輯 | ✅ 已確認並初始化 |
| **P2** | S-3 | 建立角色資料並分配員工 | ✅ 已修正 |
| **P2** | D-5 | 修正 `message_attachments` 表 COMMENT 亂碼 | ✅ 已修正 |
| **P3** | S-2 | 重新啟用模組權限系統 | ✅ 已修正 |
| **P3** | S-1 | 設定環境變數覆蓋 DB 憑證 | ✅ 已修正 |
| **P4** | S-4 | 收緊 CSP（移除 unsafe-eval） | ✅ 已部分修正 |
| **P4** | D-4 | 修正 `lookup_domains.id=0` 異常 | ✅ 已修正 |
| **P4** | D-3 | 雙狀態欄位規範確認 | ✅ 已確認 |
| **P4** | D-6 | `order_items` 軟刪除設計確認 | ✅ 已確認 |
| **P4** | A-1 | `report_descriptions` 模組命名一致性 | ✅ 已修正（2026-03-01） |
| **P4** | A-2 | `screening_items/services` 獨立 delete.php | ✅ 已修正（2026-03-01） |
| **P4** | A-3 | HTTP 方法 POST Fallback 移除 | ✅ 已修正（2026-03-01） |
| 延期 | F-1 | 拆分 `work_orders.js` | 🔴 待計劃 |

---

*本報告由 AI 程式碼審查自動生成，最後更新：2026-03-01（S-1~S-6 修正於 2026-03-01，D-1~D-6 修正於 2026-03-01，A-1~A-3 修正於 2026-03-01）*
